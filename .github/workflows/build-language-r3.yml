# Overlay builds are retired. The complete pipeline is standalone.yml.
name: Legacy overlay archive (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Overlay releases are retired. Run Standalone complete portable release instead.'
