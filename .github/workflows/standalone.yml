name: Standalone complete portable release
on:
  workflow_dispatch:
    inputs:
      publish:
        type: boolean
        description: Publish the verified complete package
        default: false
  push:
    branches: [main]
    paths:
      - '.github/workflows/standalone.yml'
      - 'release.json'
      - 'src/**'
      - 'portable_config/**'
      - 'animejanai/**'
      - 'tools/standalone/**'
      - 'tools/language-r3/**'
      - 'tests/**'
      - 'docs/standalone.md'
permissions:
  contents: write
concurrency:
  group: animejanai-standalone
  cancel-in-progress: true
jobs:
  full:
    runs-on: windows-2022
    timeout-minutes: 60
    env:
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@v4
        with: {persist-credentials: false}
      - name: Prepare playback and RTX 5080 Laptop sources
        run: |
          python tools/standalone/prepare_playback.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python tools/standalone/prepare_gpu_target.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python tools/standalone/prepare_startup.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python tests/test_gpu_target.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Verify release-page shortcut is absent
        run: python tests/test_no_release_shortcut.py
      - name: Test runtime verification
        run: |
          New-Item -ItemType Directory -Force complete-evidence | Out-Null
          python tools/standalone/test_runtime_probe.py 2>&1 | Tee-Object complete-evidence/runtime-verifier-tests.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - uses: actions/setup-dotnet@v4
        with: {dotnet-version: '10.x'}
      - name: Prepare UI sources
        run: python tools/standalone/build.py prepare
      - name: Verify languages and external argument parser
        run: |
          dotnet run --project player/tests/ScopedCommandLine -c Release 2>&1 | Tee-Object language-evidence/parser-tests.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          dotnet run --project tests-generated/manager-tests/ManagerTests.csproj -c Release -- language-evidence 2>&1 | Tee-Object language-evidence/manager-tests.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          dotnet run --project tests-generated/player-tests/PlayerTests.csproj -c Release -- language-evidence 2>&1 | Tee-Object language-evidence/player-tests.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Compile self-contained Windows applications
        run: |
          dotnet publish player/src/MpvNet.Windows/MpvNet.Windows.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:InformationalVersion=7.1.2.0-sunuuc-full-1.1.4 -o publish-player
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          dotnet publish manager/AnimeJaNaiConfEditor/AnimeJaNaiConfEditor.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:InformationalVersion=0.6.0-sunuuc-full-1.1.4 -o publish-manager
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          dotnet publish tools/standalone/Updater.csproj -c Release -o publish-updater
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Assemble RTX 5080 Laptop runtime, models and licenses
        run: python tools/standalone/build.py stage
      - name: Verify SM120-only kernels and native dependencies
        run: python tests/test_gpu_target.py stage complete-evidence
      - name: FPS, external playlist and controller tests
        run: |
          Start-Service AudioEndpointBuilder
          Start-Service Audiosrv
          python tests/test_windows_r2.py stage complete-evidence/runtime
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python tests/test_script_suite.py stage complete-evidence/runtime
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Hills input, layout, track and danmaku tests on Windows
        run: python tests/test_hills_windows.py stage complete-evidence/hills
      - name: Local HTTP playback and request-count regressions
        run: python tests/test_network_playback.py stage complete-evidence/network
      - name: External startup without original directory or PATH dependencies
        run: python tests/test_startup_playback.py stage complete-evidence/startup
      - name: Package and test files extracted into an empty directory
        run: python tools/standalone/build.py package
      - name: Publish and verify public downloads
        if: ${{ success() && ((github.event_name == 'push' && contains(github.event.head_commit.message, '[release]')) || (github.event_name == 'workflow_dispatch' && inputs.publish)) }}
        env: {GH_TOKEN: '${{ github.token }}'}
        run: python tools/standalone/publish.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: standalone-validation
          path: |
            complete-evidence/**/*.json
            complete-evidence/**/*.log
            complete-evidence/**/*.png
            language-evidence/*.txt
            language-evidence/*.png
            dist/*.json
            dist/*.txt
            dist/RELEASE.md
          if-no-files-found: warn
          retention-days: 14
      - uses: actions/upload-artifact@v4
        if: ${{ success() && !(github.event_name == 'push' && contains(github.event.head_commit.message, '[release]')) && !inputs.publish }}
        with:
          name: standalone-complete-package
          path: dist/*full.7z*
          compression-level: 0
          if-no-files-found: error
          retention-days: 3
