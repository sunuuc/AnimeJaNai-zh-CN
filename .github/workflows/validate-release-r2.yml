# The original r2 pipeline is retained in git history; no new overlay is produced.
name: Legacy r2 archive (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use standalone.yml for the complete portable distribution.'
